## What changed

## How I checked it

- [ ] `npx tsc -p tsconfig.json`
- [ ] `npm run check:anims` says `same` for everything that existed before (or explain an intended change / `NEW` entries)
- [ ] Tried it in the playground, plus the host it touches (extension, desktop, `<clawd-button>`, overlay)
- [ ] README / DEVLOG updated if people can see the change
